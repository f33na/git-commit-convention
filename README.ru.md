# 📋 Git Commit Convention

> Лёгкий и практичный стандарт оформления коммитов и семантического версионирования — подходит как для одного разработчика, так и для команды.

[![Standard Version](https://img.shields.io/badge/standard-v1.0.0-blue)](./GIT_CONVENTION_RU.md)
[![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![EN](https://img.shields.io/badge/lang-EN-blue)](./README.md)

---

## Зачем этот стандарт?

Большинство соглашений по коммитам (например, Conventional Commits) ориентированы на крупные команды с CI/CD и автоматическими changelog'ами. Этот стандарт другой:

- ✅ Работает для **одного разработчика** на небольшом проекте
- ✅ Работает для **команды** с трекером задач и ветками
- ✅ Остаётся **читаемым** в `git log` без лишних инструментов
- ✅ Напрямую отображается на **Семантическое версионирование (SemVer)**

---

## Формат коммита

```
[TASK-ID/TAG] Краткое описание изменения
```

### Два режима

**Полный формат** — есть задача и отдельная ветка:

```
[AUTH-42/FEATURE] Добавить OAuth2 авторизацию через Google
[PAY-7/FIX] Исправить округление суммы при конвертации валют
[API-15/MAJOR] Изменить структуру ответа /users endpoint
```

**Упрощённый формат** — маленький проект, нет трекера, коммит идёт в `main`/`dev`:

```
[INIT] Инициализация проекта
[FEATURE] Добавить тёмную тему
[FIX] Убрать утечку памяти в WebSocket handler
[RELEASE] v1.3.0
```

> Имя ветки всегда совпадает с `TASK-ID`.

---

## Теги изменений

| Тег        | Описание                                | Влияние на SemVer       |
| ---------- | --------------------------------------- | ----------------------- |
| `INIT`     | Инициализация проекта или модуля        | —                       |
| `FEATURE`  | Новая функциональность                  | **MINOR** ↑             |
| `FIX`      | Исправление бага                        | **PATCH** ↑             |
| `HOTFIX`   | Срочное исправление на продакшене       | **PATCH** ↑             |
| `MAJOR`    | Ломающее изменение, несовместимость API | **MAJOR** ↑             |
| `REFACTOR` | Рефакторинг без изменения поведения     | **PATCH** ↑             |
| `PERF`     | Улучшение производительности            | **PATCH** / **MINOR** ↑ |
| `STYLE`    | Форматирование, линтинг, отступы        | —                       |
| `DOCS`     | Документация                            | —                       |
| `TEST`     | Добавление или правка тестов            | —                       |
| `CHORE`    | Зависимости, конфиги, CI/CD             | —                       |
| `REVERT`   | Откат предыдущего коммита               | **PATCH** ↑             |
| `RELEASE`  | Релизный коммит, простановка версии     | —                       |

---

## Семантическое версионирование

Формат версии: **`MAJOR.MINOR.PATCH`**

Смотришь все коммиты с момента последнего тега `RELEASE` и применяешь первое подходящее правило:

```
Есть хотя бы один MAJOR?           →  MAJOR++,  MINOR = 0, PATCH = 0
Есть хотя бы один FEATURE / PERF*? →  MINOR++,  PATCH = 0
Есть FIX / HOTFIX / …?             →  PATCH++
Только STYLE / DOCS / CHORE        →  версия не меняется
```

> `*` `PERF` повышает `MINOR` только если изменение значимо для пользователя.

---

## Процесс релиза

```bash
# 1. Закончил работу — фиксируем релиз
git commit --allow-empty -m "[RELEASE] v1.3.0"

# 2. Создаём аннотированный тег
git tag -a v1.3.0 -m "Release v1.3.0"

# 3. Пушим вместе с тегами
git push origin main --tags
```

---

## Правила оформления описания

- Длина — не более **72 символов**
- Используй **повелительное наклонение**: _Добавить_, _Исправить_, _Удалить_
- Не ставь точку в конце
- Если нужен подробный контекст — добавь тело коммита через пустую строку:

```
[AUTH-42/FEATURE] Добавить OAuth2 авторизацию через Google

Реализована авторизация через Google OAuth2.
Добавлены переменные окружения GOOGLE_CLIENT_ID и GOOGLE_CLIENT_SECRET.
Затронуты модули: auth, config, middleware.
```

---

## Быстрая шпаргалка

```
Есть ветка / задача?
  ДА  →  [TASK-ID/TAG] Описание
  НЕТ →  [TAG] Описание

TAG влияет на версию?
  MAJOR    →  x+1.0.0
  FEATURE  →  x.y+1.0
  FIX      →  x.y.z+1
  прочие   →  без изменений
```

---

## Полная документация

- Основная [GIT_CONVENTION.md](./GIT_CONVENTION.md)
- 🇬🇧 [GIT_CONVENTION_EN.md](./i18n/GIT_CONVENTION_EN.md)
- 🇷🇺 [GIT_CONVENTION_RU.md](./i18n/GIT_CONVENTION_RU.md)

---

## Участие в проекте

Нашёл проблему или есть предложение? Открой [issue](../../issues) или отправь [pull request](../../pulls).  
Пожалуйста, оформляй коммиты по этому же стандарту 😉

---

## Лицензия

[MIT](LICENSE)
